{
  "name": "supabase-sms-webhook",
  "version": "1.0.0",
  "description": "SMS webhook for Supabase Auth with Russian SMS providers",
  "main": "api/send-sms.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}
